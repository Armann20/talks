# Notes on a series of talks about programming 📢

## Code as data

Under the macros folder you'll find:

* An example of infix syntax via macros in Clojure /`clojure -M macros/Infix\ in\ Clojure/infix.clj`/
* An example of prefix syntax via macros in Scala /`scala-cli run macros/Prefix\ in\ Scala .`/

**NOTE:** Use scala 3

## OOP concetps implemented with functions

TBD
